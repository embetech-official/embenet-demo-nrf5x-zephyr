# SPDX-FileCopyrightText: 2026 Embetech sp. z o.o.
# SPDX-License-Identifier: Apache-2.0

CONFIG_ZTEST=y

# ports/common only - no role, so no prebuilt library is pulled in and the test runs on native_sim.
CONFIG_EMBENET=y

# No role is selected, so the queues would otherwise be sized 0 and compiled out.
CONFIG_EMBENET_AIO_QUEUE_FRAME_COUNT=4
